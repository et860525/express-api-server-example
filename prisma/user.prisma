model User {
    id        Int      @id @default(autoincrement())
    email     String   @unique
    password  String
    role      Int      @default(0)
    createdAt DateTime
    updatedAt DateTime
}
