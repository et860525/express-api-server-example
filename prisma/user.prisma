model User {
  id        Int      @id @default(autoincrement())
  username  String   @unique
  email     String?  @unique
  password  String
  role      Int?     @default(0)
  createdAt DateTime
  updatedAt DateTime
}
